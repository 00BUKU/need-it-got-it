
    <section id="stores-page">
        <div>STORE:</div>
        <div>
          <%= store.name %>
        </div>
        <br>
        <br>


        <h2>Items</h2>
      
        <% if (user) { %>
          <form id="add-items-form" method="POST" action="/stores/<%= store._id %>/items">
            <label>Store:</label>
            <textarea name="content"></textarea>
            <input type="submit" value="NEED IT">
          </form>
          <% } else { %>
      
      
            <% } %>
      
              <% if (store.items) { %>
                <table>
                  <thead>
                    <tr>
                      <th>NEED IT</th>
                    </tr>
                  </thead>
                  <tbody>
                    <% let total=0 %>
                      <% store.items.forEach(function(r) { %>
      
                          <tr>
                            <td class="items-user"><img alt="avatar" src="<%= r.userAvatar %>" referrerpolicy="no-referrer" ><%= r.userName %></td>
                            <td>
                              <%= r.createdAt.toLocaleDateString() %>
                            </td>
                            <td>
                              <%= r.content %>
                            </td>
                            <td>
                              <% if (user && user._id.equals(r.user)) { %>
                                <form action="/items/<%= r._id %>?_method=DELETE" method="POST">
                                  <button type="submit">GOT IT</button>
                                </form>
                              <% } %>
                            </td>
                    
                
                          </tr>
                          <% }); %>
                            <tr>
                              <td colspan="2"></td>
                              <td><strong>
                                  <%= (total / store.items.length).toFixed(1) %>
                                </strong></td>
                            </tr>
                  </tbody>
                </table>
                <% } else { %>
                  <h5>No Items Yet</h5>
                  <% } %>
</body>
</html>